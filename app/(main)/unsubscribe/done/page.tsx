export default function UnsubscribeDonePage() {
  return (
    <main className="mx-auto flex min-h-[50vh] max-w-lg flex-col justify-center px-4 py-16">
      <h1 className="font-heading text-3xl font-semibold">You are unsubscribed</h1>
      <p className="mt-3 text-muted-foreground">
        You will no longer receive promotional or winner sales emails from Locals Choice Awards.
        Transactional order and account messages may still be sent when required.
      </p>
    </main>
  );
}
